# Mantua.AI — Subgraph & Analytics Layer
## Tasks P2-001 through P2-006

---

## OVERVIEW

Build a complete on-chain analytics layer for Mantua.AI with three parts:

**Part 1 — Subgraphs (P2-001 to P2-005)**
Two subgraphs deployed to The Graph Studio — one per chain:
- `mantua-base-sepolia` — indexes Base Sepolia (chainId 84532)
- `mantua-unichain-sepolia` — indexes Unichain Sepolia (chainId 1301)

Each subgraph indexes all three Mantua contracts:
- `MantuaVault` (deposits, withdrawals, yield accrual)
- `MantuaPredictionMarket` (bets, resolutions, claims)
- Uniswap v4 `PoolManager` (swaps, liquidity events)

**Part 2 — GraphQL Hooks (P2-006)**
Frontend hooks that query both subgraphs and expose typed data.
Pre-built queries for the most common analytics patterns.

**Part 3 — AI Query Engine + Chart Renderer**
The flagship feature: user types natural language in chat →
Claude generates a GraphQL query → executes against subgraph →
result rendered as an inline chart inside the chat message stream.

Supported chart types: line, bar, pie/donut, data table, stat cards.

---

## MANDATORY FIRST STEPS

Before writing any code:
1. Create `docs/tasks/012-subgraph-analytics.md`
2. Create branch `012-subgraph-analytics`
3. Read these files before modifying anything:
   - `src/utils/commandParser.ts` — extend, do not replace
   - Chat message handler — understand how `component` field works
   - `src/config/contracts.ts` — read deployed contract addresses

---

## TASK DOCUMENT

Create `docs/tasks/012-subgraph-analytics.md`:

```markdown
# Task 012: Subgraph & On-Chain Analytics

## Description
Deploy two subgraphs to The Graph Studio indexing MantuaVault,
MantuaPredictionMarket, and Uniswap v4 PoolManager on both testnets.
Build AI-powered natural language → GraphQL → inline chart pipeline.

## Task IDs
P2-001, P2-002, P2-003, P2-004, P2-005, P2-006

## Success Criteria
- [ ] subgraph.yaml valid for Base Sepolia, indexes all 3 contracts
- [ ] subgraph.yaml valid for Unichain Sepolia, indexes all 3 contracts
- [ ] Swap entity: id, pool, sender, amount0, amount1, timestamp, txHash
- [ ] Pool entity: id, token0, token1, feeTier, tvl, volumeUSD, txCount
- [ ] Position entity: id, owner, pool, liquidity, depositedToken0/1
- [ ] VaultDeposit entity: id, vault, user, assets, shares, timestamp
- [ ] PredictionBet entity: id, market, user, isYes, amount, timestamp
- [ ] Both subgraphs deployed and syncing in Graph Studio
- [ ] useSubgraph hook queries both chains, merges results
- [ ] 8 pre-built query hooks returning typed data
- [ ] Chat: "show swap volume this week" → bar chart appears inline
- [ ] Chat: "ETH price over 30 days" → line chart appears inline
- [ ] Chat: "my prediction positions" → data table appears inline
- [ ] Chat: "vault TVL breakdown" → pie chart appears inline
- [ ] Chat: "total swaps today" → stat card appears inline
- [ ] AI correctly generates GraphQL for at least 10 test queries
- [ ] Charts render with recharts, responsive, dark/light mode aware
- [ ] No TypeScript errors; all files ≤ 150 lines

## Failure Conditions
- Subgraph fails to compile (graph codegen errors)
- Entities missing required fields causing null crashes in UI
- AI generates invalid GraphQL that fails at runtime
- Chart renders with undefined/NaN data
- Inline chat chart breaks the message scroll container
- Query timeout > 5 seconds with no loading state

## Edge Cases
- Subgraph still syncing: show "Indexing…" with block progress
- Empty query result: show "No data found for this query"
- AI cannot determine chart type: default to data table
- User asks about chain A while connected to chain B: query both
- GraphQL query returns > 1000 rows: truncate with warning
```

---

## PROMPT A — SUBGRAPH SCHEMA (P2-001 to P2-004)

### A1. Project Structure

```
subgraph/
├── base-sepolia/
│   ├── subgraph.yaml
│   ├── schema.graphql
│   ├── package.json
│   └── src/
│       ├── vault.ts
│       ├── prediction-market.ts
│       ├── pool-manager.ts
│       └── helpers.ts
└── unichain-sepolia/
    ├── subgraph.yaml          # same schema, different addresses
    ├── schema.graphql         # identical to base-sepolia
    ├── package.json
    └── src/                   # identical mappings, symlinked or copied
        ├── vault.ts
        ├── prediction-market.ts
        ├── pool-manager.ts
        └── helpers.ts
```

### A2. GraphQL Schema (P2-002, P2-003, P2-004)

Create `subgraph/base-sepolia/schema.graphql`
(copy identically to `subgraph/unichain-sepolia/schema.graphql`):

```graphql
# ─── SWAP ENTITIES (P2-002) ──────────────────────────────────────────

"""
Individual swap event from Uniswap v4 PoolManager.
"""
type Swap @entity(immutable: true) {
  id:          Bytes!          # tx hash + log index
  pool:        Pool!
  sender:      Bytes!          # address
  recipient:   Bytes!
  amount0:     BigDecimal!     # token0 delta (negative = out)
  amount1:     BigDecimal!     # token1 delta
  amountUSD:   BigDecimal!
  sqrtPriceX96: BigInt!
  tick:        Int!
  timestamp:   BigInt!
  blockNumber: BigInt!
  txHash:      Bytes!
}

"""
Hourly swap volume snapshot for time-series charts.
"""
type SwapHourData @entity {
  id:          String!         # pool address + hour timestamp
  pool:        Pool!
  hourStartUnix: BigInt!
  volumeUSD:   BigDecimal!
  volumeToken0: BigDecimal!
  volumeToken1: BigDecimal!
  txCount:     BigInt!
}

# ─── POOL ENTITIES (P2-003) ──────────────────────────────────────────

"""
Uniswap v4 liquidity pool.
"""
type Pool @entity {
  id:           Bytes!         # poolId from PoolManager
  token0:       Token!
  token1:       Token!
  feeTier:      BigInt!
  hooks:        Bytes!         # hook contract address
  liquidity:    BigInt!
  sqrtPrice:    BigInt!
  tick:         Int!
  tvlUSD:       BigDecimal!
  volumeUSD:    BigDecimal!
  feesUSD:      BigDecimal!
  txCount:      BigInt!
  createdAt:    BigInt!
  positions:    [Position!]! @derivedFrom(field: "pool")
  swaps:        [Swap!]!      @derivedFrom(field: "pool")
  hourData:     [SwapHourData!]! @derivedFrom(field: "pool")
}

type Token @entity {
  id:           Bytes!         # token address
  symbol:       String!
  name:         String!
  decimals:     Int!
  derivedETH:   BigDecimal!
  totalValueLocked: BigDecimal!
  volume:       BigDecimal!
  txCount:      BigInt!
}

# ─── POSITION ENTITIES (P2-004) ──────────────────────────────────────

"""
Liquidity provider position in a Uniswap v4 pool.
"""
type Position @entity {
  id:               Bytes!      # positionId from PoolManager
  owner:            Bytes!
  pool:             Pool!
  tickLower:        Int!
  tickUpper:        Int!
  liquidity:        BigInt!
  depositedToken0:  BigDecimal!
  depositedToken1:  BigDecimal!
  withdrawnToken0:  BigDecimal!
  withdrawnToken1:  BigDecimal!
  collectedFeesToken0: BigDecimal!
  collectedFeesToken1: BigDecimal!
  createdAt:        BigInt!
  transaction:      Bytes!
}

# ─── VAULT ENTITIES ──────────────────────────────────────────────────

"""
ERC-4626 vault registry — one entity per deployed MantuaVault.
"""
type Vault @entity {
  id:            Bytes!          # vault contract address
  name:          String!
  symbol:        String!
  asset:         Bytes!          # LP token address
  strategy:      Int!            # 0=LP, 1=Stable, 2=AI
  totalAssets:   BigDecimal!
  totalShares:   BigDecimal!
  apyBps:        BigInt!
  deposits:      [VaultDeposit!]!    @derivedFrom(field: "vault")
  withdrawals:   [VaultWithdrawal!]! @derivedFrom(field: "vault")
  dailyData:     [VaultDayData!]!    @derivedFrom(field: "vault")
}

type VaultDeposit @entity(immutable: true) {
  id:         Bytes!
  vault:      Vault!
  sender:     Bytes!
  owner:      Bytes!
  assets:     BigDecimal!
  shares:     BigDecimal!
  timestamp:  BigInt!
  blockNumber: BigInt!
  txHash:     Bytes!
}

type VaultWithdrawal @entity(immutable: true) {
  id:         Bytes!
  vault:      Vault!
  sender:     Bytes!
  receiver:   Bytes!
  owner:      Bytes!
  assets:     BigDecimal!
  shares:     BigDecimal!
  timestamp:  BigInt!
  txHash:     Bytes!
}

type VaultDayData @entity {
  id:          String!         # vault address + day timestamp
  vault:       Vault!
  dayStartUnix: BigInt!
  totalAssets: BigDecimal!
  totalShares: BigDecimal!
  dailyDeposits: BigDecimal!
  dailyWithdrawals: BigDecimal!
  apyBps:      BigInt!
}

# ─── PREDICTION MARKET ENTITIES ──────────────────────────────────────

type PredictionMarket @entity {
  id:             Bytes!         # market ID as bytes
  contractId:     BigInt!        # on-chain market ID
  question:       String!
  category:       String!
  endTime:        BigInt!
  resolved:       Boolean!
  outcome:        Boolean        # null until resolved
  totalYesShares: BigDecimal!
  totalNoShares:  BigDecimal!
  bets:           [PredictionBet!]! @derivedFrom(field: "market")
  claims:         [PredictionClaim!]! @derivedFrom(field: "market")
  createdAt:      BigInt!
}

type PredictionBet @entity(immutable: true) {
  id:         Bytes!
  market:     PredictionMarket!
  user:       Bytes!
  isYes:      Boolean!
  amount:     BigDecimal!        # USDC amount (6 decimals normalized)
  shares:     BigDecimal!
  timestamp:  BigInt!
  txHash:     Bytes!
}

type PredictionClaim @entity(immutable: true) {
  id:         Bytes!
  market:     PredictionMarket!
  user:       Bytes!
  payout:     BigDecimal!
  timestamp:  BigInt!
  txHash:     Bytes!
}

# ─── GLOBAL STATS ────────────────────────────────────────────────────

"""
Single row — id is always "global". Updated on every event.
"""
type Protocol @entity {
  id:                  String!    # "global"
  totalVolumeUSD:      BigDecimal!
  totalFeesUSD:        BigDecimal!
  totalTvlUSD:         BigDecimal!
  totalSwaps:          BigInt!
  totalPools:          BigInt!
  totalVaultDeposits:  BigDecimal!
  totalBetsUSD:        BigDecimal!
  updatedAt:           BigInt!
}

type ProtocolDayData @entity {
  id:           String!           # "global-" + day timestamp
  dayStartUnix: BigInt!
  volumeUSD:    BigDecimal!
  feesUSD:      BigDecimal!
  tvlUSD:       BigDecimal!
  txCount:      BigInt!
}
```

### A3. subgraph.yaml (P2-001, P2-005)

Create `subgraph/base-sepolia/subgraph.yaml`:

```yaml
specVersion: 1.0.0
indexerHints:
  prune: auto

schema:
  file: ./schema.graphql

dataSources:
  # ── Uniswap v4 PoolManager ──────────────────────────────────────────
  - kind: ethereum
    name: PoolManager
    network: base-sepolia
    source:
      # Official Uniswap v4 PoolManager on Base Sepolia
      address: "0x05E73354cFDd6745C338b50BcFDfA3Aa6fA03408"
      abi: PoolManager
      startBlock: 18000000    # approximate deploy block — adjust after checking
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Pool
        - Token
        - Swap
        - Position
        - SwapHourData
        - Protocol
        - ProtocolDayData
      abis:
        - name: PoolManager
          file: ./abis/PoolManager.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: Initialize(indexed bytes32,indexed address,indexed address,uint24,int24,address)
          handler: handleInitialize
        - event: Swap(indexed bytes32,indexed address,int128,int128,uint160,uint128,int24,uint24)
          handler: handleSwap
        - event: ModifyLiquidity(indexed bytes32,indexed address,int24,int24,int256,bytes32)
          handler: handleModifyLiquidity
      file: ./src/pool-manager.ts

  # ── MantuaVault ─────────────────────────────────────────────────────
  # Note: one dataSource per deployed vault instance.
  # Add all 4 vault addresses after deployment.
  - kind: ethereum
    name: MantuaVaultETHUSDC
    network: base-sepolia
    source:
      address: "0xVAULT_ETH_USDC_BASE_SEPOLIA"   # replace after deploy
      abi: MantuaVault
      startBlock: 18000000
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Vault
        - VaultDeposit
        - VaultWithdrawal
        - VaultDayData
        - Protocol
      abis:
        - name: MantuaVault
          file: ./abis/MantuaVault.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
      file: ./src/vault.ts

  - kind: ethereum
    name: MantuaVaultUSDCUSDT
    network: base-sepolia
    source:
      address: "0xVAULT_USDC_USDT_BASE_SEPOLIA"
      abi: MantuaVault
      startBlock: 18000000
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Vault
        - VaultDeposit
        - VaultWithdrawal
        - VaultDayData
        - Protocol
      abis:
        - name: MantuaVault
          file: ./abis/MantuaVault.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
      file: ./src/vault.ts

  - kind: ethereum
    name: MantuaVaultETHBTC
    network: base-sepolia
    source:
      address: "0xVAULT_ETH_BTC_BASE_SEPOLIA"
      abi: MantuaVault
      startBlock: 18000000
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Vault
        - VaultDeposit
        - VaultWithdrawal
        - VaultDayData
        - Protocol
      abis:
        - name: MantuaVault
          file: ./abis/MantuaVault.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
      file: ./src/vault.ts

  - kind: ethereum
    name: MantuaVaultAIMulti
    network: base-sepolia
    source:
      address: "0xVAULT_AI_MULTI_BASE_SEPOLIA"
      abi: MantuaVault
      startBlock: 18000000
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Vault
        - VaultDeposit
        - VaultWithdrawal
        - VaultDayData
        - Protocol
      abis:
        - name: MantuaVault
          file: ./abis/MantuaVault.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
      file: ./src/vault.ts

  # ── MantuaPredictionMarket ───────────────────────────────────────────
  - kind: ethereum
    name: MantuaPredictionMarket
    network: base-sepolia
    source:
      address: "0xMANTUA_PM_BASE_SEPOLIA"        # replace after deploy
      abi: MantuaPredictionMarket
      startBlock: 18000000
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - PredictionMarket
        - PredictionBet
        - PredictionClaim
        - Protocol
      abis:
        - name: MantuaPredictionMarket
          file: ./abis/MantuaPredictionMarket.json
      eventHandlers:
        - event: MarketCreated(indexed uint256,string,string,uint256,address)
          handler: handleMarketCreated
        - event: SharesBought(indexed uint256,indexed address,bool,uint256,uint256)
          handler: handleSharesBought
        - event: MarketResolved(indexed uint256,bool)
          handler: handleMarketResolved
        - event: WinningsClaimed(indexed uint256,indexed address,uint256)
          handler: handleWinningsClaimed
      file: ./src/prediction-market.ts
```

Create `subgraph/unichain-sepolia/subgraph.yaml` — identical structure,
replace `network: base-sepolia` → `network: unichain-sepolia` and all
contract addresses with Unichain equivalents.

### A4. AssemblyScript Mappings

**`subgraph/base-sepolia/src/helpers.ts`** (≤ 150 lines):

```typescript
import { BigDecimal, BigInt, Bytes, Address } from '@graphprotocol/graph-ts';
import { Protocol, Token } from '../generated/schema';
import { ERC20 } from '../generated/PoolManager/ERC20';

export const ZERO_BD  = BigDecimal.fromString('0');
export const ONE_BD   = BigDecimal.fromString('1');
export const ZERO_BI  = BigInt.fromI32(0);
export const ONE_BI   = BigInt.fromI32(1);

export function getOrCreateProtocol(): Protocol {
  let protocol = Protocol.load('global');
  if (protocol === null) {
    protocol = new Protocol('global');
    protocol.totalVolumeUSD     = ZERO_BD;
    protocol.totalFeesUSD       = ZERO_BD;
    protocol.totalTvlUSD        = ZERO_BD;
    protocol.totalSwaps         = ZERO_BI;
    protocol.totalPools         = ZERO_BI;
    protocol.totalVaultDeposits = ZERO_BD;
    protocol.totalBetsUSD       = ZERO_BD;
    protocol.updatedAt          = ZERO_BI;
    protocol.save();
  }
  return protocol as Protocol;
}

export function getOrCreateToken(address: Address): Token {
  let token = Token.load(address);
  if (token === null) {
    token = new Token(address);
    let contract = ERC20.bind(address);
    let symbolResult = contract.try_symbol();
    let nameResult   = contract.try_name();
    let decimalsResult = contract.try_decimals();
    token.symbol   = symbolResult.reverted   ? 'UNKNOWN' : symbolResult.value;
    token.name     = nameResult.reverted     ? 'Unknown' : nameResult.value;
    token.decimals = decimalsResult.reverted ? 18        : decimalsResult.value;
    token.derivedETH          = ZERO_BD;
    token.totalValueLocked    = ZERO_BD;
    token.volume              = ZERO_BD;
    token.txCount             = ZERO_BI;
    token.save();
  }
  return token as Token;
}

export function getDayStartUnix(timestamp: BigInt): BigInt {
  let dayIndex = timestamp.toI32() / 86400;
  return BigInt.fromI32(dayIndex * 86400);
}

export function getHourStartUnix(timestamp: BigInt): BigInt {
  let hourIndex = timestamp.toI32() / 3600;
  return BigInt.fromI32(hourIndex * 3600);
}

export function bigIntToBigDecimal(value: BigInt, decimals: i32): BigDecimal {
  let bd = value.toBigDecimal();
  let divisor = BigDecimal.fromString('1' + '0'.repeat(decimals));
  return bd.div(divisor);
}
```

**`subgraph/base-sepolia/src/vault.ts`** (≤ 150 lines):

```typescript
import { Address, BigInt, Bytes } from '@graphprotocol/graph-ts';
import { Deposit, Withdraw } from '../generated/MantuaVaultETHUSDC/MantuaVault';
import { Vault, VaultDeposit, VaultWithdrawal, VaultDayData } from '../generated/schema';
import { ZERO_BD, ZERO_BI, getOrCreateProtocol,
         getDayStartUnix, bigIntToBigDecimal } from './helpers';

function getOrCreateVault(address: Address): Vault {
  let vault = Vault.load(address);
  if (vault === null) {
    vault = new Vault(address);
    vault.name         = '';
    vault.symbol       = '';
    vault.asset        = address;
    vault.strategy     = 0;
    vault.totalAssets  = ZERO_BD;
    vault.totalShares  = ZERO_BD;
    vault.apyBps       = ZERO_BI;
    vault.save();
  }
  return vault as Vault;
}

function getOrCreateVaultDayData(vault: Vault, timestamp: BigInt): VaultDayData {
  let dayStart = getDayStartUnix(timestamp);
  let id = vault.id.toHex() + '-' + dayStart.toString();
  let dayData = VaultDayData.load(id);
  if (dayData === null) {
    dayData = new VaultDayData(id);
    dayData.vault             = vault.id;
    dayData.dayStartUnix      = dayStart;
    dayData.totalAssets       = vault.totalAssets;
    dayData.totalShares       = vault.totalShares;
    dayData.dailyDeposits     = ZERO_BD;
    dayData.dailyWithdrawals  = ZERO_BD;
    dayData.apyBps            = vault.apyBps;
    dayData.save();
  }
  return dayData as VaultDayData;
}

export function handleDeposit(event: Deposit): void {
  let vault    = getOrCreateVault(event.address);
  let protocol = getOrCreateProtocol();
  let assets   = bigIntToBigDecimal(event.params.assets, 18);
  let shares   = bigIntToBigDecimal(event.params.shares, 18);

  // Update vault totals
  vault.totalAssets = vault.totalAssets.plus(assets);
  vault.totalShares = vault.totalShares.plus(shares);
  vault.save();

  // Update day data
  let dayData = getOrCreateVaultDayData(vault, event.block.timestamp);
  dayData.totalAssets   = vault.totalAssets;
  dayData.totalShares   = vault.totalShares;
  dayData.dailyDeposits = dayData.dailyDeposits.plus(assets);
  dayData.save();

  // Create immutable deposit entity
  let depositId = event.transaction.hash.concatI32(event.logIndex.toI32());
  let deposit   = new VaultDeposit(depositId);
  deposit.vault       = vault.id;
  deposit.sender      = event.params.sender;
  deposit.owner       = event.params.owner;
  deposit.assets      = assets;
  deposit.shares      = shares;
  deposit.timestamp   = event.block.timestamp;
  deposit.blockNumber = event.block.number;
  deposit.txHash      = event.transaction.hash;
  deposit.save();

  // Update protocol stats
  protocol.totalVaultDeposits = protocol.totalVaultDeposits.plus(assets);
  protocol.updatedAt          = event.block.timestamp;
  protocol.save();
}

export function handleWithdraw(event: Withdraw): void {
  let vault   = getOrCreateVault(event.address);
  let assets  = bigIntToBigDecimal(event.params.assets, 18);
  let shares  = bigIntToBigDecimal(event.params.shares, 18);

  vault.totalAssets = vault.totalAssets.minus(assets);
  vault.totalShares = vault.totalShares.minus(shares);
  vault.save();

  let dayData = getOrCreateVaultDayData(vault, event.block.timestamp);
  dayData.totalAssets      = vault.totalAssets;
  dayData.dailyWithdrawals = dayData.dailyWithdrawals.plus(assets);
  dayData.save();

  let withdrawId  = event.transaction.hash.concatI32(event.logIndex.toI32());
  let withdrawal  = new VaultWithdrawal(withdrawId);
  withdrawal.vault       = vault.id;
  withdrawal.sender      = event.params.sender;
  withdrawal.receiver    = event.params.receiver;
  withdrawal.owner       = event.params.owner;
  withdrawal.assets      = assets;
  withdrawal.shares      = shares;
  withdrawal.timestamp   = event.block.timestamp;
  withdrawal.txHash      = event.transaction.hash;
  withdrawal.save();
}
```

**`subgraph/base-sepolia/src/prediction-market.ts`** (≤ 150 lines):

```typescript
import { BigInt, Bytes } from '@graphprotocol/graph-ts';
import {
  MarketCreated, SharesBought,
  MarketResolved, WinningsClaimed
} from '../generated/MantuaPredictionMarket/MantuaPredictionMarket';
import {
  PredictionMarket, PredictionBet, PredictionClaim
} from '../generated/schema';
import { ZERO_BD, ZERO_BI, getOrCreateProtocol,
         bigIntToBigDecimal } from './helpers';

export function handleMarketCreated(event: MarketCreated): void {
  let market = new PredictionMarket(
    Bytes.fromByteArray(Bytes.fromBigInt(event.params.marketId))
  );
  market.contractId      = event.params.marketId;
  market.question        = event.params.question;
  market.category        = event.params.category;
  market.endTime         = event.params.endTime;
  market.resolved        = false;
  market.outcome         = null;
  market.totalYesShares  = ZERO_BD;
  market.totalNoShares   = ZERO_BD;
  market.createdAt       = event.block.timestamp;
  market.save();
}

export function handleSharesBought(event: SharesBought): void {
  let market = PredictionMarket.load(
    Bytes.fromByteArray(Bytes.fromBigInt(event.params.marketId))
  );
  if (market === null) return;

  let amount = bigIntToBigDecimal(event.params.amount, 6); // USDC 6 decimals

  if (event.params.isYes) {
    market.totalYesShares = market.totalYesShares.plus(amount);
  } else {
    market.totalNoShares = market.totalNoShares.plus(amount);
  }
  market.save();

  let betId = event.transaction.hash.concatI32(event.logIndex.toI32());
  let bet   = new PredictionBet(betId);
  bet.market    = market.id;
  bet.user      = event.params.user;
  bet.isYes     = event.params.isYes;
  bet.amount    = amount;
  bet.shares    = bigIntToBigDecimal(event.params.shares, 6);
  bet.timestamp = event.block.timestamp;
  bet.txHash    = event.transaction.hash;
  bet.save();

  let protocol = getOrCreateProtocol();
  protocol.totalBetsUSD = protocol.totalBetsUSD.plus(amount);
  protocol.updatedAt    = event.block.timestamp;
  protocol.save();
}

export function handleMarketResolved(event: MarketResolved): void {
  let market = PredictionMarket.load(
    Bytes.fromByteArray(Bytes.fromBigInt(event.params.marketId))
  );
  if (market === null) return;
  market.resolved = true;
  market.outcome  = event.params.outcome;
  market.save();
}

export function handleWinningsClaimed(event: WinningsClaimed): void {
  let claimId = event.transaction.hash.concatI32(event.logIndex.toI32());
  let claim   = new PredictionClaim(claimId);
  claim.market    = Bytes.fromByteArray(
    Bytes.fromBigInt(event.params.marketId)
  );
  claim.user      = event.params.user;
  claim.payout    = bigIntToBigDecimal(event.params.payout, 6);
  claim.timestamp = event.block.timestamp;
  claim.txHash    = event.transaction.hash;
  claim.save();
}
```

**`subgraph/base-sepolia/src/pool-manager.ts`** (≤ 150 lines):

```typescript
import { Address, BigInt, Bytes } from '@graphprotocol/graph-ts';
import {
  Initialize, Swap as SwapEvent, ModifyLiquidity
} from '../generated/PoolManager/PoolManager';
import { Pool, Swap, Position, SwapHourData } from '../generated/schema';
import {
  ZERO_BD, ZERO_BI, ONE_BI, getOrCreateProtocol,
  getOrCreateToken, getHourStartUnix, bigIntToBigDecimal
} from './helpers';

export function handleInitialize(event: Initialize): void {
  let pool      = new Pool(event.params.id);
  let token0    = getOrCreateToken(event.params.currency0);
  let token1    = getOrCreateToken(event.params.currency1);

  pool.token0    = token0.id;
  pool.token1    = token1.id;
  pool.feeTier   = BigInt.fromI32(event.params.fee);
  pool.hooks     = event.params.hooks;
  pool.liquidity = ZERO_BI;
  pool.sqrtPrice = event.params.sqrtPriceX96;
  pool.tick      = event.params.tick;
  pool.tvlUSD    = ZERO_BD;
  pool.volumeUSD = ZERO_BD;
  pool.feesUSD   = ZERO_BD;
  pool.txCount   = ZERO_BI;
  pool.createdAt = event.block.timestamp;
  pool.save();

  let protocol   = getOrCreateProtocol();
  protocol.totalPools = protocol.totalPools.plus(ONE_BI);
  protocol.save();
}

export function handleSwap(event: SwapEvent): void {
  let pool = Pool.load(event.params.id);
  if (pool === null) return;

  let amount0 = bigIntToBigDecimal(event.params.amount0, 18);
  let amount1 = bigIntToBigDecimal(event.params.amount1, 18);
  // Approximate USD value — production would use price feeds
  let amountUSD = amount0.abs().plus(amount1.abs());

  // Update pool
  pool.volumeUSD = pool.volumeUSD.plus(amountUSD);
  pool.txCount   = pool.txCount.plus(ONE_BI);
  pool.sqrtPrice = event.params.sqrtPriceX96;
  pool.tick      = event.params.tick;
  pool.save();

  // Hour data
  let hourStart = getHourStartUnix(event.block.timestamp);
  let hourId    = pool.id.toHex() + '-' + hourStart.toString();
  let hourData  = SwapHourData.load(hourId);
  if (hourData === null) {
    hourData = new SwapHourData(hourId);
    hourData.pool          = pool.id;
    hourData.hourStartUnix = hourStart;
    hourData.volumeUSD     = ZERO_BD;
    hourData.volumeToken0  = ZERO_BD;
    hourData.volumeToken1  = ZERO_BD;
    hourData.txCount       = ZERO_BI;
  }
  hourData.volumeUSD    = hourData.volumeUSD.plus(amountUSD);
  hourData.volumeToken0 = hourData.volumeToken0.plus(amount0.abs());
  hourData.volumeToken1 = hourData.volumeToken1.plus(amount1.abs());
  hourData.txCount      = hourData.txCount.plus(ONE_BI);
  hourData.save();

  // Create swap entity
  let swapId = event.transaction.hash.concatI32(event.logIndex.toI32());
  let swap   = new Swap(swapId);
  swap.pool        = pool.id;
  swap.sender      = event.params.sender;
  swap.recipient   = event.params.sender; // v4 sender = recipient in basic swaps
  swap.amount0     = amount0;
  swap.amount1     = amount1;
  swap.amountUSD   = amountUSD;
  swap.sqrtPriceX96 = event.params.sqrtPriceX96;
  swap.tick        = event.params.tick;
  swap.timestamp   = event.block.timestamp;
  swap.blockNumber = event.block.number;
  swap.txHash      = event.transaction.hash;
  swap.save();

  // Protocol totals
  let protocol = getOrCreateProtocol();
  protocol.totalVolumeUSD = protocol.totalVolumeUSD.plus(amountUSD);
  protocol.totalSwaps     = protocol.totalSwaps.plus(ONE_BI);
  protocol.updatedAt      = event.block.timestamp;
  protocol.save();
}

export function handleModifyLiquidity(event: ModifyLiquidity): void {
  let pool = Pool.load(event.params.id);
  if (pool === null) return;

  let posId  = event.params.id.concat(
    Bytes.fromByteArray(Bytes.fromBigInt(
      BigInt.fromI32(event.params.tickLower)
        .plus(BigInt.fromI32(event.params.tickUpper))
    ))
  );
  let position = Position.load(posId);
  if (position === null) {
    position = new Position(posId);
    position.owner             = event.params.sender;
    position.pool              = pool.id;
    position.tickLower         = event.params.tickLower;
    position.tickUpper         = event.params.tickUpper;
    position.liquidity         = ZERO_BI;
    position.depositedToken0   = ZERO_BD;
    position.depositedToken1   = ZERO_BD;
    position.withdrawnToken0   = ZERO_BD;
    position.withdrawnToken1   = ZERO_BD;
    position.collectedFeesToken0 = ZERO_BD;
    position.collectedFeesToken1 = ZERO_BD;
    position.createdAt         = event.block.timestamp;
    position.transaction       = event.transaction.hash;
  }
  let liquidityDelta = event.params.liquidityDelta;
  if (liquidityDelta.gt(ZERO_BI)) {
    position.liquidity = position.liquidity.plus(liquidityDelta);
  } else {
    let abs = liquidityDelta.abs();
    position.liquidity = position.liquidity.gt(abs)
      ? position.liquidity.minus(abs)
      : ZERO_BI;
  }
  position.save();
}
```

### A5. Package.json & Deploy Commands

Create `subgraph/base-sepolia/package.json`:

```json
{
  "name": "mantua-base-sepolia",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build":   "graph build",
    "deploy":  "graph deploy --studio mantua-base-sepolia",
    "deploy:unichain": "graph deploy --studio mantua-unichain-sepolia"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts":  "0.35.1"
  }
}
```

**Deploy steps (P2-005):**

```bash
# Install Graph CLI globally
npm install -g @graphprotocol/graph-cli@0.81.0

# Authenticate (get deploy key from https://thegraph.com/studio/)
graph auth --studio <DEPLOY_KEY>

# Base Sepolia subgraph
cd subgraph/base-sepolia
npm install
graph codegen      # generates TypeScript types from schema
graph build        # compiles to WASM — must show "Build completed"
graph deploy --studio mantua-base-sepolia

# Unichain Sepolia subgraph
cd ../unichain-sepolia
graph codegen
graph build
graph deploy --studio mantua-unichain-sepolia
```

After deployment, paste both query URLs into `.env`:

```bash
VITE_SUBGRAPH_BASE_SEPOLIA=https://api.studio.thegraph.com/query/<ID>/mantua-base-sepolia/version/latest
VITE_SUBGRAPH_UNICHAIN=https://api.studio.thegraph.com/query/<ID>/mantua-unichain-sepolia/version/latest
```

---

## PROMPT B — GRAPHQL HOOKS (P2-006)

### B1. GraphQL Client Setup

Create `src/lib/graphql.ts` (≤ 150 lines):

```ts
/**
 * graphql.ts
 * Thin GraphQL client for The Graph Studio endpoints.
 * Supports federated queries across Base Sepolia + Unichain Sepolia.
 * No heavy dependencies — uses native fetch.
 */

const ENDPOINTS = {
  baseSepolia: import.meta.env.VITE_SUBGRAPH_BASE_SEPOLIA ?? '',
  unichain:    import.meta.env.VITE_SUBGRAPH_UNICHAIN    ?? '',
} as const;

export type ChainTarget = 'baseSepolia' | 'unichain' | 'both';

export interface GraphQLResponse<T> {
  data:   T | null;
  errors: { message: string }[] | null;
}

/**
 * Execute a GraphQL query against one or both subgraphs.
 * When target='both', results are merged into a single object.
 */
export async function gqlQuery<T>(
  query:     string,
  variables: Record<string, unknown> = {},
  target:    ChainTarget = 'both'
): Promise<{ baseSepolia?: T; unichain?: T; merged?: T }> {
  const fetch1 = target !== 'unichain'
    ? queryEndpoint<T>(ENDPOINTS.baseSepolia, query, variables)
    : Promise.resolve(null);
  const fetch2 = target !== 'baseSepolia'
    ? queryEndpoint<T>(ENDPOINTS.unichain,    query, variables)
    : Promise.resolve(null);

  const [r1, r2] = await Promise.all([fetch1, fetch2]);

  return {
    baseSepolia: r1 ?? undefined,
    unichain:    r2 ?? undefined,
    merged:      mergeResults(r1, r2),
  };
}

async function queryEndpoint<T>(
  url:       string,
  query:     string,
  variables: Record<string, unknown>
): Promise<T | null> {
  if (!url) return null;
  try {
    const res = await fetch(url, {
      method:  'POST',
      headers: { 'Content-Type': 'application/json' },
      body:    JSON.stringify({ query, variables }),
    });
    const json: GraphQLResponse<T> = await res.json();
    if (json.errors?.length) {
      console.error('[Subgraph] GraphQL errors:', json.errors);
      return null;
    }
    return json.data;
  } catch (err) {
    console.error('[Subgraph] Fetch error:', err);
    return null;
  }
}

/**
 * Naive merge: concatenate arrays, sum scalars.
 * Sufficient for hackathon analytics aggregation.
 */
function mergeResults<T>(a: T | null, b: T | null): T | undefined {
  if (!a && !b) return undefined;
  if (!a) return b ?? undefined;
  if (!b) return a;

  const merged = { ...a } as Record<string, unknown>;
  for (const key of Object.keys(b as object)) {
    const va = (a as Record<string, unknown>)[key];
    const vb = (b as Record<string, unknown>)[key];
    if (Array.isArray(va) && Array.isArray(vb)) {
      merged[key] = [...va, ...vb];
    } else if (typeof va === 'number' && typeof vb === 'number') {
      merged[key] = va + vb;
    }
    // For objects (like 'protocols'), prefer first result
  }
  return merged as T;
}
```

### B2. Pre-built Query Hooks

Create `src/hooks/useSubgraph.ts` (≤ 150 lines):

```ts
/**
 * useSubgraph.ts
 * 8 pre-built typed GraphQL hooks covering the most common analytics queries.
 * Each hook queries both subgraphs and returns merged data.
 */

import { useState, useEffect } from 'react';
import { gqlQuery } from '../lib/graphql';

// ── 1. Protocol stats (stat cards) ──────────────────────────────────

const PROTOCOL_QUERY = `{
  protocols(first: 1, where: { id: "global" }) {
    totalVolumeUSD totalFeesUSD totalTvlUSD
    totalSwaps totalPools totalVaultDeposits totalBetsUSD
  }
}`;

export function useProtocolStats() {
  const [data, setData]       = useState<any>(null);
  const [isLoading, setLoad]  = useState(true);
  useEffect(() => {
    gqlQuery(PROTOCOL_QUERY).then(r => {
      setData(r.merged);
      setLoad(false);
    });
  }, []);
  return { data, isLoading };
}

// ── 2. Swap volume last 7 days (bar chart) ───────────────────────────

const SWAP_VOLUME_QUERY = `query SwapVolume($since: BigInt!) {
  swapHourDatas(
    first: 168
    where: { hourStartUnix_gte: $since }
    orderBy: hourStartUnix orderDirection: asc
  ) {
    hourStartUnix volumeUSD txCount
    pool { token0 { symbol } token1 { symbol } }
  }
}`;

export function useSwapVolume(days = 7) {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    const since = Math.floor(Date.now() / 1000) - days * 86400;
    gqlQuery(SWAP_VOLUME_QUERY, { since: since.toString() }).then(r => {
      setData((r.merged as any)?.swapHourDatas ?? []);
      setLoad(false);
    });
  }, [days]);
  return { data, isLoading };
}

// ── 3. Pool TVL list (table / bar chart) ────────────────────────────

const POOLS_QUERY = `{
  pools(first: 20, orderBy: tvlUSD, orderDirection: desc) {
    id tvlUSD volumeUSD feesUSD txCount
    token0 { symbol } token1 { symbol } feeTier
  }
}`;

export function usePools() {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    gqlQuery(POOLS_QUERY).then(r => {
      setData((r.merged as any)?.pools ?? []);
      setLoad(false);
    });
  }, []);
  return { data, isLoading };
}

// ── 4. Vault deposits over time (line chart) ─────────────────────────

const VAULT_HISTORY_QUERY = `query VaultHistory($since: BigInt!) {
  vaultDayDatas(
    first: 30
    where: { dayStartUnix_gte: $since }
    orderBy: dayStartUnix orderDirection: asc
  ) {
    dayStartUnix totalAssets dailyDeposits dailyWithdrawals
    vault { name symbol strategy }
  }
}`;

export function useVaultHistory(days = 30) {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    const since = Math.floor(Date.now() / 1000) - days * 86400;
    gqlQuery(VAULT_HISTORY_QUERY, { since: since.toString() }).then(r => {
      setData((r.merged as any)?.vaultDayDatas ?? []);
      setLoad(false);
    });
  }, [days]);
  return { data, isLoading };
}

// ── 5. User positions (table) ────────────────────────────────────────

const USER_POSITIONS_QUERY = `query UserPositions($user: Bytes!) {
  positions(where: { owner: $user }) {
    id liquidity tickLower tickUpper
    depositedToken0 depositedToken1
    pool { token0 { symbol } token1 { symbol } tvlUSD feeTier }
  }
  vaultDeposits(where: { owner: $user }, first: 20) {
    assets shares timestamp
    vault { name symbol apyBps }
  }
  predictionBets(where: { user: $user }, first: 20) {
    amount isYes timestamp
    market { question resolved outcome }
  }
}`;

export function useUserPositions(address: string | undefined) {
  const [data, setData]      = useState<any>(null);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    if (!address) { setLoad(false); return; }
    gqlQuery(USER_POSITIONS_QUERY, { user: address.toLowerCase() }).then(r => {
      setData(r.merged);
      setLoad(false);
    });
  }, [address]);
  return { data, isLoading };
}

// ── 6. Prediction market bets (bar / pie chart) ──────────────────────

const PREDICTION_BETS_QUERY = `{
  predictionMarkets(first: 20, orderBy: createdAt, orderDirection: desc) {
    id question category resolved outcome
    totalYesShares totalNoShares
    bets(first: 5, orderBy: timestamp, orderDirection: desc) {
      amount isYes timestamp
    }
  }
}`;

export function usePredictionMarkets() {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    gqlQuery(PREDICTION_BETS_QUERY).then(r => {
      setData((r.merged as any)?.predictionMarkets ?? []);
      setLoad(false);
    });
  }, []);
  return { data, isLoading };
}

// ── 7. Recent swaps (table) ──────────────────────────────────────────

const RECENT_SWAPS_QUERY = `{
  swaps(first: 50, orderBy: timestamp, orderDirection: desc) {
    id amountUSD timestamp txHash
    pool { token0 { symbol } token1 { symbol } }
  }
}`;

export function useRecentSwaps() {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    gqlQuery(RECENT_SWAPS_QUERY).then(r => {
      setData((r.merged as any)?.swaps ?? []);
      setLoad(false);
    });
  }, []);
  return { data, isLoading };
}

// ── 8. Vault TVL breakdown (pie chart) ───────────────────────────────

const VAULT_TVL_QUERY = `{
  vaults(first: 10, orderBy: totalAssets, orderDirection: desc) {
    id name symbol strategy totalAssets totalShares apyBps
  }
}`;

export function useVaultTvl() {
  const [data, setData]      = useState<any[]>([]);
  const [isLoading, setLoad] = useState(true);
  useEffect(() => {
    gqlQuery(VAULT_TVL_QUERY).then(r => {
      setData((r.merged as any)?.vaults ?? []);
      setLoad(false);
    });
  }, []);
  return { data, isLoading };
}
```

---

## PROMPT C — AI QUERY ENGINE + CHART RENDERER

### C1. Natural Language → GraphQL Engine

Create `src/lib/analyticsEngine.ts` (≤ 150 lines):

```ts
/**
 * analyticsEngine.ts
 * Converts natural language analytics queries to GraphQL via Claude API.
 * Uses system prompt with full schema context so Claude generates valid queries.
 * Classifies chart type from query intent.
 */

export type ChartType = 'line' | 'bar' | 'pie' | 'table' | 'stat';

export interface AnalyticsQuery {
  graphql:     string;        // generated GraphQL query string
  chartType:   ChartType;
  title:       string;        // human-readable title for chart header
  description: string;        // one-sentence explanation of what is shown
  variables:   Record<string, string>;
}

const SCHEMA_CONTEXT = `
You are a GraphQL query generator for the Mantua.AI subgraph.
Available entities and fields:

Protocol: totalVolumeUSD, totalFeesUSD, totalTvlUSD, totalSwaps, totalPools,
          totalVaultDeposits, totalBetsUSD

Swap: id, pool{token0{symbol} token1{symbol}}, amountUSD, timestamp, txHash

SwapHourData: pool{id token0{symbol} token1{symbol}}, hourStartUnix,
              volumeUSD, volumeToken0, volumeToken1, txCount

Pool: id, token0{symbol name}, token1{symbol name}, feeTier, tvlUSD,
      volumeUSD, feesUSD, txCount, createdAt

Position: id, owner, pool{token0{symbol} token1{symbol}}, liquidity,
          tickLower, tickUpper, depositedToken0, depositedToken1

Vault: id, name, symbol, strategy, totalAssets, totalShares, apyBps

VaultDeposit: vault{name symbol}, sender, owner, assets, shares, timestamp

VaultDayData: vault{name}, dayStartUnix, totalAssets, dailyDeposits,
              dailyWithdrawals, apyBps

PredictionMarket: id, question, category, resolved, outcome,
                  totalYesShares, totalNoShares, createdAt

PredictionBet: market{question}, user, isYes, amount, timestamp

PredictionClaim: market{question}, user, payout, timestamp

Filtering: where:{field_gte: $value, field_lte: $value}
Sorting: orderBy: fieldName, orderDirection: asc|desc
Pagination: first: N, skip: N
Timestamps are Unix seconds (BigInt as string in variables)

Return ONLY a JSON object with these exact fields:
{
  "graphql": "{ ... }",
  "chartType": "line|bar|pie|table|stat",
  "title": "Human readable title",
  "description": "One sentence description",
  "variables": {}
}
Choose chartType based on:
- line: time-series data (hourly/daily trends)
- bar: comparison across categories (pools, markets, vaults)
- pie: distribution/breakdown (shares, allocations)
- table: list of records, user-specific data
- stat: single aggregate number
`;

export async function generateAnalyticsQuery(
  userMessage: string
): Promise<AnalyticsQuery | null> {
  try {
    const response = await fetch('https://api.anthropic.com/v1/messages', {
      method:  'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model:      'claude-sonnet-4-20250514',
        max_tokens: 1000,
        system:     SCHEMA_CONTEXT,
        messages:   [{ role: 'user', content: userMessage }],
      }),
    });

    const data  = await response.json();
    const text  = data.content
      ?.filter((b: any) => b.type === 'text')
      .map((b: any) => b.text)
      .join('') ?? '';

    const clean = text.replace(/```json|```/g, '').trim();
    return JSON.parse(clean) as AnalyticsQuery;
  } catch (err) {
    console.error('[AnalyticsEngine] Failed to generate query:', err);
    return null;
  }
}

/**
 * Detect if a chat message is an analytics query (not a trade command).
 * Returns true when message contains data/analytics keywords.
 */
export function isAnalyticsQuery(message: string): boolean {
  const msg = message.toLowerCase();
  const analyticsKeywords = [
    'show', 'chart', 'graph', 'plot', 'volume', 'tvl', 'stats',
    'analytics', 'history', 'over time', 'breakdown', 'distribution',
    'how much', 'how many', 'total', 'trend', 'compare', 'performance',
    'bets', 'swaps', 'positions', 'deposits', 'withdrawals', 'yield',
    'top pools', 'top vaults', 'my portfolio', 'leaderboard',
  ];
  const tradeKeywords = ['swap', 'buy', 'sell', 'deposit', 'withdraw', 'bet'];

  const hasAnalytics = analyticsKeywords.some(k => msg.includes(k));
  const hasTrade     = tradeKeywords.some(k => msg.startsWith(k));
  return hasAnalytics && !hasTrade;
}
```

### C2. Chart Renderer Component

Create `src/components/analytics/ChartMessage.tsx` (≤ 150 lines):

```tsx
/**
 * ChartMessage.tsx
 * Renders an analytics result inline inside a chat message bubble.
 * Supports: line, bar, pie, table, stat card.
 * Uses recharts for all chart types.
 * Matches chat message bubble styling exactly.
 */

import {
  LineChart, Line, BarChart, Bar, PieChart, Pie, Cell,
  XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend
} from 'recharts';
import type { ChartType } from '../../lib/analyticsEngine';

const COLORS = ['#7c3aed', '#3b82f6', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6'];

interface Props {
  title:       string;
  description: string;
  chartType:   ChartType;
  data:        any[];
  isLoading:   boolean;
  error:       string | null;
}

export function ChartMessage({ title, description, chartType,
                               data, isLoading, error }: Props) {
  return (
    // Matches assistant message bubble width and styling
    <div className="w-full max-w-2xl rounded-2xl border border-gray-800
                    bg-gray-900 overflow-hidden">
      {/* Header */}
      <div className="px-4 py-3 border-b border-gray-800">
        <p className="text-sm font-semibold text-white">{title}</p>
        <p className="text-xs text-gray-500 mt-0.5">{description}</p>
      </div>

      {/* Content area */}
      <div className="p-4">
        {isLoading && (
          <div className="flex items-center justify-center h-40 gap-3 text-gray-500">
            <div className="w-4 h-4 border-2 border-purple-500
                            border-t-transparent rounded-full animate-spin" />
            <span className="text-sm">Querying subgraph…</span>
          </div>
        )}
        {error && !isLoading && (
          <div className="flex items-center justify-center h-40 text-red-400 text-sm">
            {error}
          </div>
        )}
        {!isLoading && !error && data.length === 0 && (
          <div className="flex items-center justify-center h-40 text-gray-500 text-sm">
            No data found for this query.
          </div>
        )}
        {!isLoading && !error && data.length > 0 && (
          <>
            {chartType === 'line'  && <LineChartView  data={data} />}
            {chartType === 'bar'   && <BarChartView   data={data} />}
            {chartType === 'pie'   && <PieChartView   data={data} />}
            {chartType === 'table' && <TableView      data={data} />}
            {chartType === 'stat'  && <StatCardView   data={data} />}
          </>
        )}
      </div>

      {/* Footer — subgraph credit */}
      <div className="px-4 py-2 border-t border-gray-800 text-xs text-gray-600">
        Powered by The Graph · Live on-chain data
      </div>
    </div>
  );
}

function LineChartView({ data }: { data: any[] }) {
  const keys = data.length > 0
    ? Object.keys(data[0]).filter(k => k !== 'name' && k !== 'timestamp'
        && k !== 'hourStartUnix' && k !== 'dayStartUnix')
    : [];
  return (
    <ResponsiveContainer width="100%" height={200}>
      <LineChart data={data} margin={{ top: 5, right: 10, bottom: 5, left: 0 }}>
        <CartesianGrid strokeDasharray="3 3" stroke="#1f2937" />
        <XAxis dataKey="name" tick={{ fill: '#6b7280', fontSize: 11 }} />
        <YAxis tick={{ fill: '#6b7280', fontSize: 11 }} />
        <Tooltip contentStyle={{ background: '#111827', border: '1px solid #374151',
                                  color: '#fff', fontSize: 12 }} />
        {keys.map((k, i) => (
          <Line key={k} type="monotone" dataKey={k}
                stroke={COLORS[i % COLORS.length]} strokeWidth={2} dot={false} />
        ))}
      </LineChart>
    </ResponsiveContainer>
  );
}

function BarChartView({ data }: { data: any[] }) {
  const keys = data.length > 0
    ? Object.keys(data[0]).filter(k => k !== 'name')
    : [];
  return (
    <ResponsiveContainer width="100%" height={200}>
      <BarChart data={data} margin={{ top: 5, right: 10, bottom: 5, left: 0 }}>
        <CartesianGrid strokeDasharray="3 3" stroke="#1f2937" />
        <XAxis dataKey="name" tick={{ fill: '#6b7280', fontSize: 11 }} />
        <YAxis tick={{ fill: '#6b7280', fontSize: 11 }} />
        <Tooltip contentStyle={{ background: '#111827', border: '1px solid #374151',
                                  color: '#fff', fontSize: 12 }} />
        {keys.map((k, i) => (
          <Bar key={k} dataKey={k} fill={COLORS[i % COLORS.length]}
               radius={[4, 4, 0, 0]} />
        ))}
      </BarChart>
    </ResponsiveContainer>
  );
}

function PieChartView({ data }: { data: any[] }) {
  return (
    <ResponsiveContainer width="100%" height={200}>
      <PieChart>
        <Pie data={data} cx="50%" cy="50%" innerRadius={50} outerRadius={80}
             dataKey="value" nameKey="name" paddingAngle={3}>
          {data.map((_, i) => (
            <Cell key={i} fill={COLORS[i % COLORS.length]} />
          ))}
        </Pie>
        <Tooltip contentStyle={{ background: '#111827', border: '1px solid #374151',
                                  color: '#fff', fontSize: 12 }} />
        <Legend formatter={(v) => (
          <span className="text-xs text-gray-400">{v}</span>
        )} />
      </PieChart>
    </ResponsiveContainer>
  );
}

function TableView({ data }: { data: any[] }) {
  const cols = data.length > 0 ? Object.keys(data[0]) : [];
  const rows = data.slice(0, 20); // cap at 20 rows
  return (
    <div className="overflow-x-auto max-h-56 overflow-y-auto">
      <table className="w-full text-xs">
        <thead className="sticky top-0 bg-gray-900">
          <tr>{cols.map(c => (
            <th key={c} className="text-left text-gray-500 font-medium
                                   pb-2 pr-4 whitespace-nowrap capitalize">
              {c}
            </th>
          ))}</tr>
        </thead>
        <tbody className="divide-y divide-gray-800">
          {rows.map((row, i) => (
            <tr key={i}>{cols.map(c => (
              <td key={c} className="py-2 pr-4 text-gray-300 whitespace-nowrap
                                     font-mono max-w-32 truncate">
                {String(row[c] ?? '—')}
              </td>
            ))}</tr>
          ))}
        </tbody>
      </table>
      {data.length > 20 && (
        <p className="text-xs text-gray-600 mt-2">
          Showing 20 of {data.length} results
        </p>
      )}
    </div>
  );
}

function StatCardView({ data }: { data: any[] }) {
  const stats = Array.isArray(data) ? data : Object.entries(data[0] ?? {});
  return (
    <div className="grid grid-cols-2 gap-3">
      {stats.slice(0, 4).map(([k, v]: [string, any], i) => (
        <div key={i} className="p-3 bg-gray-800 rounded-xl">
          <p className="text-xs text-gray-500 mb-1 capitalize">{k}</p>
          <p className="text-xl font-bold text-white">{String(v)}</p>
        </div>
      ))}
    </div>
  );
}
```

### C3. Data Normalizer

Create `src/lib/normalizeSubgraphData.ts` (≤ 150 lines):

```ts
/**
 * normalizeSubgraphData.ts
 * Transforms raw subgraph query results into chart-ready arrays.
 * Detects entity type and applies appropriate field mappings.
 */

import type { ChartType } from './analyticsEngine';

export interface ChartDataPoint {
  name:  string;
  [key: string]: string | number;
}

export function normalizeForChart(
  raw:       any,
  chartType: ChartType
): ChartDataPoint[] {
  if (!raw || typeof raw !== 'object') return [];

  // Find the first array in the result object
  const key  = Object.keys(raw).find(k => Array.isArray(raw[k]));
  const rows = key ? raw[key] : [];

  if (rows.length === 0) {
    // Handle Protocol single-object case (stat cards)
    if (raw.protocols?.[0]) return normalizeProtocol(raw.protocols[0]);
    return [];
  }

  // Detect entity type from first row shape
  const first = rows[0];

  if ('hourStartUnix'  in first) return normalizeHourData(rows);
  if ('dayStartUnix'   in first) return normalizeDayData(rows);
  if ('totalYesShares' in first) return normalizePredictionMarkets(rows);
  if ('apyBps'         in first) return normalizeVaults(rows, chartType);
  if ('amountUSD'      in first) return normalizeSwaps(rows);
  if ('tvlUSD'         in first) return normalizePools(rows);
  if ('liquidity'      in first) return normalizePositions(rows);
  if ('txHash'         in first) return normalizeDeposits(rows);

  return rows.map((r: any, i: number) => ({ name: String(i + 1), ...r }));
}

function normalizeHourData(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:      formatTimestamp(Number(r.hourStartUnix), 'hour'),
    volumeUSD: parseFloat(r.volumeUSD ?? '0'),
    txCount:   Number(r.txCount ?? 0),
  }));
}

function normalizeDayData(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:       formatTimestamp(Number(r.dayStartUnix), 'day'),
    totalAssets: parseFloat(r.totalAssets ?? '0'),
    deposits:   parseFloat(r.dailyDeposits ?? '0'),
    withdrawals: parseFloat(r.dailyWithdrawals ?? '0'),
  }));
}

function normalizePredictionMarkets(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:      r.question?.slice(0, 40) + '…' ?? 'Market',
    yesShares: parseFloat(r.totalYesShares ?? '0'),
    noShares:  parseFloat(r.totalNoShares  ?? '0'),
    value:     parseFloat(r.totalYesShares ?? '0') +
               parseFloat(r.totalNoShares  ?? '0'),
  }));
}

function normalizeVaults(rows: any[], chartType: ChartType): ChartDataPoint[] {
  return rows.map(r => ({
    name:   r.symbol ?? r.name ?? 'Vault',
    value:  parseFloat(r.totalAssets ?? '0'),
    apy:    Number(r.apyBps ?? 0) / 100,
    shares: parseFloat(r.totalShares ?? '0'),
  }));
}

function normalizeSwaps(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:   formatTimestamp(Number(r.timestamp), 'time'),
    usd:    parseFloat(r.amountUSD ?? '0'),
    pair:   `${r.pool?.token0?.symbol}/${r.pool?.token1?.symbol}` ?? '—',
    tx:     (r.txHash ?? '').slice(0, 8) + '…',
  }));
}

function normalizePools(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:   `${r.token0?.symbol}/${r.token1?.symbol}`,
    tvl:    parseFloat(r.tvlUSD  ?? '0'),
    volume: parseFloat(r.volumeUSD ?? '0'),
    fees:   parseFloat(r.feesUSD ?? '0'),
    value:  parseFloat(r.tvlUSD  ?? '0'),
  }));
}

function normalizePositions(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:   `${r.pool?.token0?.symbol}/${r.pool?.token1?.symbol}` ?? 'Position',
    token0: parseFloat(r.depositedToken0 ?? '0'),
    token1: parseFloat(r.depositedToken1 ?? '0'),
    value:  parseFloat(r.depositedToken0 ?? '0'),
  }));
}

function normalizeDeposits(rows: any[]): ChartDataPoint[] {
  return rows.map(r => ({
    name:   formatTimestamp(Number(r.timestamp), 'day'),
    amount: parseFloat(r.assets ?? r.amount ?? '0'),
    vault:  r.vault?.symbol ?? '—',
    value:  parseFloat(r.assets ?? '0'),
  }));
}

function normalizeProtocol(p: any): ChartDataPoint[] {
  return Object.entries(p)
    .filter(([k]) => k !== '__typename')
    .map(([k, v]) => ({
      name:  formatKey(k),
      value: String(v),
    }));
}

function formatTimestamp(ts: number, precision: 'hour' | 'day' | 'time'): string {
  const d = new Date(ts * 1000);
  if (precision === 'time') return d.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' });
  if (precision === 'hour') return d.toLocaleString('en-US', { month: 'short', day: 'numeric', hour: '2-digit' });
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
}

function formatKey(k: string): string {
  return k.replace(/([A-Z])/g, ' $1').replace(/^./, s => s.toUpperCase()).trim();
}
```

### C4. Chat Integration

In the existing chat message handler, add analytics routing.
Find where messages are processed and add this block **before** the
existing trade command parsers:

```tsx
// In ChatInterface.tsx or wherever handleSendMessage() lives
import { isAnalyticsQuery, generateAnalyticsQuery } from '../lib/analyticsEngine';
import { gqlQuery }               from '../lib/graphql';
import { normalizeForChart }      from '../lib/normalizeSubgraphData';

// At the top of handleSendMessage():
if (isAnalyticsQuery(message)) {
  // 1. Add user message to chat
  addMessage({ role: 'user', content: message });

  // 2. Add loading chart placeholder
  const chartMsgId = `chart-${Date.now()}`;
  addMessage({
    id:        chartMsgId,
    role:      'assistant',
    content:   '',
    component: 'chart',
    chartData: {
      title:       'Querying…',
      description: 'Generating GraphQL from your question…',
      chartType:   'table',
      data:        [],
      isLoading:   true,
      error:       null,
    },
  });

  // 3. Generate GraphQL via Claude
  const analyticsQuery = await generateAnalyticsQuery(message);
  if (!analyticsQuery) {
    updateMessage(chartMsgId, {
      chartData: {
        title:       'Error',
        description: 'Could not generate a query for this question.',
        chartType:   'table',
        data:        [],
        isLoading:   false,
        error:       'Try rephrasing — e.g. "show swap volume this week"',
      },
    });
    return;
  }

  // 4. Execute against subgraph
  const result = await gqlQuery(
    analyticsQuery.graphql,
    analyticsQuery.variables,
    'both'
  );

  // 5. Normalize data for chart
  const chartData = normalizeForChart(result.merged, analyticsQuery.chartType);

  // 6. Update chart message with real data
  updateMessage(chartMsgId, {
    chartData: {
      title:       analyticsQuery.title,
      description: analyticsQuery.description,
      chartType:   analyticsQuery.chartType,
      data:        chartData,
      isLoading:   false,
      error:       chartData.length === 0 ? 'No data found.' : null,
    },
  });
  return;
}
```

In the chat message renderer (where messages are mapped to JSX),
add the chart component case:

```tsx
// In MessageList.tsx or equivalent
import { ChartMessage } from '../analytics/ChartMessage';

// Inside message render:
{msg.component === 'chart' && msg.chartData && (
  <ChartMessage
    title={msg.chartData.title}
    description={msg.chartData.description}
    chartType={msg.chartData.chartType}
    data={msg.chartData.data}
    isLoading={msg.chartData.isLoading}
    error={msg.chartData.error}
  />
)}
```

### C5. Pre-Built Query Library

Create `src/components/analytics/QueryLibrary.tsx` (≤ 150 lines):

```tsx
/**
 * QueryLibrary.tsx
 * Pre-built analytics queries displayed as chips below the chat input.
 * Clicking a chip populates the input and auto-submits the query.
 * Shown only when the chat has no active trade interface open.
 */

import { BarChart2, TrendingUp, PieChart, Table, Hash } from 'lucide-react';

interface Query {
  label:     string;
  message:   string;
  icon:      typeof BarChart2;
  chartType: string;
}

const PRESET_QUERIES: Query[] = [
  { label: 'Swap volume this week',   message: 'Show swap volume by hour for the last 7 days',
    icon: BarChart2,  chartType: 'bar'   },
  { label: 'Vault TVL breakdown',     message: 'Show vault TVL distribution across all vaults',
    icon: PieChart,   chartType: 'pie'   },
  { label: 'Pool performance',        message: 'Show top 10 pools by TVL',
    icon: Table,      chartType: 'table' },
  { label: 'Prediction market bets',  message: 'Show yes vs no bets across all prediction markets',
    icon: BarChart2,  chartType: 'bar'   },
  { label: 'Vault deposits over 30d', message: 'Show daily vault deposits over the last 30 days',
    icon: TrendingUp, chartType: 'line'  },
  { label: 'Total protocol stats',    message: 'Show total protocol stats: volume, TVL, swaps',
    icon: Hash,       chartType: 'stat'  },
  { label: 'My positions',            message: 'Show all my liquidity positions and vault deposits',
    icon: Table,      chartType: 'table' },
  { label: 'Recent swaps',            message: 'Show the last 20 swaps across all pools',
    icon: Table,      chartType: 'table' },
];

interface Props {
  onSelect: (message: string) => void;
  visible:  boolean;
}

export function QueryLibrary({ onSelect, visible }: Props) {
  if (!visible) return null;
  return (
    <div className="px-4 pb-3">
      <p className="text-xs text-gray-600 mb-2 font-medium uppercase tracking-wide">
        Analytics Queries
      </p>
      <div className="flex flex-wrap gap-2">
        {PRESET_QUERIES.map(q => (
          <button
            key={q.label}
            onClick={() => onSelect(q.message)}
            className="flex items-center gap-1.5 px-3 py-1.5
                       bg-gray-800 hover:bg-gray-700 border border-gray-700
                       hover:border-gray-600 rounded-xl text-xs text-gray-300
                       hover:text-white transition-all"
          >
            <q.icon className="w-3 h-3 text-purple-400" />
            {q.label}
          </button>
        ))}
      </div>
    </div>
  );
}
```

Add `<QueryLibrary>` just above the chat input, passing `onSelect`
to populate and submit the input field.

---

## IMPLEMENTATION ORDER

Execute in this exact sequence:

```
1.  Create docs/tasks/012-subgraph-analytics.md
2.  Create branch: 012-subgraph-analytics
3.  mkdir -p subgraph/base-sepolia/src subgraph/base-sepolia/abis
4.  Copy MantuaVault.json, MantuaPredictionMarket.json, PoolManager.json
    into subgraph/base-sepolia/abis/  (get PoolManager ABI from Uniswap v4 repo)
5.  Create schema.graphql
6.  Create subgraph.yaml (replace placeholder addresses with real ones)
7.  Create src/helpers.ts, src/vault.ts, src/prediction-market.ts,
    src/pool-manager.ts
8.  Run: npm install && graph codegen  (must succeed with zero errors)
9.  Run: graph build                   (must output "Build completed")
10. Run: graph deploy --studio mantua-base-sepolia
11. Repeat steps 3-10 for unichain-sepolia
12. Paste both query URLs into .env
13. Create src/lib/graphql.ts
14. Create src/lib/analyticsEngine.ts
15. Create src/lib/normalizeSubgraphData.ts
16. Create src/hooks/useSubgraph.ts
17. Create src/components/analytics/ChartMessage.tsx
18. Create src/components/analytics/QueryLibrary.tsx
19. Integrate chat handler (C4)
20. Add <QueryLibrary> above chat input
21. Verify recharts is installed: npm list recharts
    If not: npm install recharts@2.12.0
22. Run: npx tsc --noEmit   (zero errors)
23. Run: npm run lint        (zero warnings)
24. Manual test: type each preset query, verify chart renders
25. Commit with detailed message
```

---

## VERIFICATION CHECKLIST

**Subgraph**
- [ ] `graph codegen` succeeds for base-sepolia
- [ ] `graph build` succeeds for base-sepolia
- [ ] Graph Studio shows subgraph syncing (not "Failed")
- [ ] `graph build` succeeds for unichain-sepolia
- [ ] Graph Studio shows both subgraphs syncing
- [ ] Entities visible in Graph Studio playground: Protocol, Pool, Swap,
      Vault, VaultDeposit, PredictionMarket, PredictionBet

**Pre-built hooks**
- [ ] `useProtocolStats()` returns object with totalVolumeUSD
- [ ] `useSwapVolume()` returns array of hour data
- [ ] `usePools()` returns array of pools
- [ ] `useVaultTvl()` returns array of vaults with totalAssets
- [ ] `usePredictionMarkets()` returns array of markets
- [ ] `useRecentSwaps()` returns array of swaps
- [ ] `useUserPositions(address)` returns positions, deposits, bets

**AI Query Engine**
- [ ] `isAnalyticsQuery('show swap volume')` returns true
- [ ] `isAnalyticsQuery('swap 1 ETH for USDC')` returns false
- [ ] `generateAnalyticsQuery('show swap volume this week')` returns valid JSON
- [ ] Generated GraphQL passes `graph-ts` validation format

**Chart Renderer**
- [ ] Line chart renders with XAxis, YAxis, tooltip
- [ ] Bar chart renders with correct colors
- [ ] Pie chart renders with legend
- [ ] Table renders with column headers, scrollable
- [ ] Stat cards render in 2-column grid
- [ ] Loading spinner shows during query
- [ ] Empty state message shows when data is []

**Chat Integration**
- [ ] "show swap volume this week" → bar chart in chat
- [ ] "vault TVL breakdown" → pie chart in chat
- [ ] "my positions" → table in chat
- [ ] "total swaps today" → stat card in chat
- [ ] "ETH price over 30 days" → line chart in chat
- [ ] Chart does not break chat scroll container
- [ ] QueryLibrary chips visible above input
- [ ] Clicking chip populates and submits input

---

## ENVIRONMENT VARIABLES

Add to `.env`:
```bash
VITE_SUBGRAPH_BASE_SEPOLIA=https://api.studio.thegraph.com/query/<ID>/mantua-base-sepolia/version/latest
VITE_SUBGRAPH_UNICHAIN=https://api.studio.thegraph.com/query/<ID>/mantua-unichain-sepolia/version/latest
```

---

## COMMIT MESSAGE

```
feat(analytics): complete subgraph + AI query engine P2-001 through P2-006

Subgraphs (Base Sepolia + Unichain Sepolia):
- schema.graphql: 14 entities covering Protocol, Pool, Token, Swap,
  SwapHourData, Position, Vault, VaultDeposit, VaultWithdrawal, VaultDayData,
  PredictionMarket, PredictionBet, PredictionClaim, ProtocolDayData
- subgraph.yaml: indexes PoolManager (swaps, pools, positions),
  4 MantuaVault instances (deposits, withdrawals), MantuaPredictionMarket
- AssemblyScript mappings: vault.ts, prediction-market.ts, pool-manager.ts
- helpers.ts: getOrCreateProtocol, getOrCreateToken, time helpers
- Both subgraphs deployed and syncing in The Graph Studio

Frontend (P2-006):
- graphql.ts: native fetch client, federated queries across 2 chains,
  result merging with array concat + scalar sum
- useSubgraph.ts: 8 pre-built typed hooks (protocol stats, swap volume,
  pools, vault history, user positions, prediction markets, recent swaps,
  vault TVL)
- analyticsEngine.ts: natural language → GraphQL via Claude API,
  schema context in system prompt, chartType classification,
  isAnalyticsQuery() intent detector
- normalizeSubgraphData.ts: entity-type detection, transforms raw
  subgraph arrays into recharts-compatible ChartDataPoint[]
- ChartMessage.tsx: inline chat chart component, 5 chart types
  (line, bar, pie, table, stat), loading state, empty state
- QueryLibrary.tsx: 8 preset query chips rendered above chat input

Chat integration:
- Analytics query routing: isAnalyticsQuery check before trade parsers
- Streaming update: loading placeholder → real chart on data arrival
- updateMessage() pattern for in-place message mutation

Files added: 8 (src/), 6 (subgraph/)
TypeScript errors: 0
Subgraphs: 2 deployed, syncing
```

---

## DO NOT

- ❌ Do not install Apollo Client — use native fetch in graphql.ts
- ❌ Do not skip `graph codegen` — generated types are required for mappings
- ❌ Do not hardcode subgraph URLs — read from `import.meta.env`
- ❌ Do not use `any[]` for chart data in ChartMessage props — use ChartDataPoint[]
- ❌ Do not route analytics queries through swap/liquidity parsers
- ❌ Do not render charts as full-page modals — they must render inline in chat
- ❌ Do not install a separate charting library — recharts is already available
- ❌ Do not create files over 150 lines — split into modules
- ❌ Do not skip the task document as the first step
- ❌ Do not commit with failing `graph build` — it must succeed before deploy